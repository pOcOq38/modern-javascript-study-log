// Wrapper object
// converts a primitive type to an object when needed
const number = 123;
console.log(number.toString());
console.log(number); //primitive number type

const text = "text"; //primitive string type
text.length; //property from String object wrapper
text.trim(); //method from String object wrapper
